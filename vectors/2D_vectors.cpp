// vector<vector<int>>vector-name(n,m);
// where n is the number of rows and m is the size of vector in each row.
// vector of vector datatype

// 2-D vector of n,m;
// vector<vector<int>>V(n,vector<int>(m));

// if we want to initialise the 2-D vector with some value say a;
// vector<vector<int>>V(n,vector<int>(m,a));

