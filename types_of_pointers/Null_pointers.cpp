#include<iostream>
using namespace std;

int main(){
    int *ptr=nullptr;
    int *ptr1=0; // because zero is a special reserved memory address in many operating system.
    int *ptr2='\0';

    // NULL pointers 
    // if we want to declare a pointer , but assign it's value later then we can use NULL pointer.
    // run time error might occur on dereferencing the pointer.
    return 0;
}